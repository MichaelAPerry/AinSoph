# Godot build output
.godot/
build/

# .NET
bin/
obj/
*.user

# Model files — too large for git (bundle in PCK at export time only)
*.gguf
*.ggml
*.bin

# Keep the models/ directory itself
!models/.gdkeep

# OS
.DS_Store
Thumbs.db
